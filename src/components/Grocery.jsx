import React from "react";

const Grocery = () => {
  return (
    <div className="text-2xl font-serif m-16">
      :- I have made this component for lazy loading which is known as code
      splitting or Dynamic bundling or dynamic import or chunking also where we
      are split the code into another code . where we can use in large scale
      app.
    </div>
  );
};

export default Grocery;
